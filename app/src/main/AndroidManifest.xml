<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidassesmenttest"

    tools:ignore="LockedOrientationActivity">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.VenueApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAssesmentTest">
        <activity

            android:name=".presentation.venueDetailsActivity.ui.VenueDetailsActivity"
            android:exported="false"
            android:label="@string/title_activity_venue_details"
            android:theme="@style/Theme.AndroidAssesmentTest" />
        <activity

            android:name=".presentation.searchVenuesActivity.ui.VenueActivity"
            android:exported="true"
            android:theme="@style/Theme.AndroidAssesmentTest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>